blocks_dir: output/blocks
results_dir: output/results
templates_path: templates.yaml

provider:
  default: openrouter

openrouter:
  base_url: https://openrouter.ai/api/v1
  api_key_env: OPENROUTER_API_KEY

ollama:
  base_url: http://localhost:11434

models:
  generate: openrouter:minimax/minimax-m3:free
  mutate: openrouter:minimax/minimax-m3:free
  compose: openrouter:minimax/minimax-m3:free
  naming: ollama:qwen3:1.7b
  local_default: ollama:qwen3:4b

google:
  scopes:
    - https://www.googleapis.com/auth/documents.readonly
  credentials_path: credentials/credentials.json
  token_path: credentials/token.json

constraints:
  generate: prompt_constraints/GENERATE_CONSTRAINTS.md
  naming: prompt_constraints/NAMING_CONSTRAINTS.md
  mutate: prompt_constraints/MUTATE_CONSTRAINTS.md
  compose: prompt_constraints/COMPOSE_CONSTRAINTS.md
